import { StyleSheet, Text, View } from 'react-native';

export default function HouseScreen() {
   return (
      <View className="flex-1 justify-center items-center">
         <Text>This is the HouseScreen component</Text>
      </View>
   );
}
